#include<stdio.h>
#include<conio.h>

void dia(int **a,int row)
{

  int i,j,sum=0,sum1=0,dif;
  printf("Enter Array elements");
   for(i=0;i<row;i++)
	{
	  for(j=0;j<row;j++)
		{
		  scanf("%d",&a[i][j]);
		}
	}
  printf("\n");
   for(i=0;i<row;i++)
	{
	  for(j=0;j<row;j++)
		{
		  printf("   %d",a[i][j]);
		}
	  printf("\n");
	}
  j=0;
  for(i=0;i<row;i++)
  {
    sum=sum+a[i][j];
    j++;

  }
  j=row-1;
  for(i=0;i<row;i++)
  {
    sum1=sum1+a[i][j];
    j--;

  }
  dif=sum-sum1;
  printf("\nDifference=%d",dif);
  getch();
}
int main(int arg,char *argv[])
{
  int **dyn;
  int row,col,i;
  clrscr();
  printf("Enter number of rows");
  scanf("%d",&row);
  dyn=(int*)malloc(row*(sizeof(int*)));
  for(i=0;i<row;i++)
  dyn[i]=(int*)malloc(row*sizeof(int*));
  dia(dyn,row);
}